CREATE TABLE `tbl_package_purchase` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL,
  `package_id` int(10) unsigned NOT NULL,
  `period` int(11) DEFAULT NULL,
  `price` decimal(6,2) DEFAULT '0.00',
  `date_added` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `date_paid` timestamp NULL DEFAULT NULL,
  `paid` smallint(6) NOT NULL DEFAULT '0',
  `date_modified` timestamp NULL DEFAULT NULL,
  `status` smallint(6) NOT NULL DEFAULT '0',
  `date_expire` timestamp NULL DEFAULT NULL,
  `date_start` timestamp NULL DEFAULT NULL,
  `expire_days_msg` int(11) NOT NULL DEFAULT '0',
  `force_activation` smallint(6) NOT NULL DEFAULT '0',
  `t_id` text,
  `t_status` smallint(6) NOT NULL DEFAULT '0',
  `t_date` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `user_id` (`user_id`),
  KEY `package_id` (`package_id`),
  CONSTRAINT `tbl_package_purchase_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `tbl_user` (`id`) ON DELETE CASCADE,
  CONSTRAINT `tbl_package_purchase_ibfk_2` FOREIGN KEY (`package_id`) REFERENCES `tbl_package` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8